export interface MiWeb {
  header: {
    title: string;
    textColor: string;
    textFamily: string;
    backgroundMode: string;
    backgroundColor: string;
    logoUrl?: string;
    backgroundImageUrl?: string;
  };
}
